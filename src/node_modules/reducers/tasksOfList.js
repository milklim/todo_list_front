import {
    append
} from 'ramda'

import {
    FETCH_LIST_TASKS_SUCCESS,
} from 'actionTypes'

const initialState = []

export default (state = initialState, {type, payload}) => {
    switch (type) {
        case FETCH_LIST_TASKS_SUCCESS:
            // const lists = state;
            // lists.map((list) => list.tasks.push(payload))
            // // console.log('reducers.tasks', payload)
            // console.log('reducers.tasksOfList', lists)
            return
        default:
            return state
    }
}