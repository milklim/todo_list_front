import React, {Component} from 'react'
import {connect} from 'react-redux'

import {
    fetchListTasks
} from 'actions'

import {
    getTasksOfList
} from "selectors";


class TasksOfList extends Component {
    componentDidMount () {
        this.props.fetchListTasks(this.props.listId) //param
    }

    render() {
        return (
            <div>
                TasksOfLists
            </div>
        )
    }
}


// const mapStateToProps = (state, listId) => {
//     console.log('props.listId', this.props.listId)
//
//     return {
//         tasks: getTasksOfList(state, 1)
//     }
// }

const mapDispatchToProps = {
    fetchListTasks
}

export default connect(null, mapDispatchToProps)(TasksOfList)